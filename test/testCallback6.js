const callback = require('../callback6');

const path = ['boards', 'lists', 'cards'];

const thanosBoardId = 'mcu453ed';
const cardListId = ['qwsa221', 'jwkh245', 'azxs123', 'cffv432', 'ghnb768'];

callback(path, thanosBoardId, cardListId);
